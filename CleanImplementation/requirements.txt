# Core dependencies for Clean Implementation
# No mock data libraries - only real computation

# Numerical computation
numpy>=1.20.0
scipy>=1.7.0

# Visualization (for real data only)
matplotlib>=3.4.0

# Optional: Distributed computing
# Uncomment if you want MPI support
# mpi4py>=3.0.0

# Optional: Better progress tracking
# tqdm>=4.60.0

# Note: This implementation does NOT require:
# - faker (no fake data generation)
# - mock (no mock objects)
# - Any simulation-specific libraries

# All results come from actual algorithm execution